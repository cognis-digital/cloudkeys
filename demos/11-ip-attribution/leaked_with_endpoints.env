# Example config leaked alongside cloud endpoints.
# Every secret below is FAKE, shaped like the real format. The IPs are real
# CIDRs from the AWS/GCP published ranges, used only to demo attribution.

APP_ENV=production

# --- leaked AWS credential ---
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# --- endpoints the app talks to (these get attributed to a cloud) ---
API_ENDPOINT=https://service.internal/3.4.12.4
DATA_HOST=34.1.208.1
INTERNAL_VPN=10.0.0.1
